import 'package:flutter/material.dart';

class BindHouse extends StatelessWidget {
  const BindHouse({Key key}) : super(key: key);
  static const String routeName = '/bindhouse';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
