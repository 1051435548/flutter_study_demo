import 'package:flutter/material.dart';

class BindUser extends StatelessWidget {
  const BindUser({Key key}) : super(key: key);
  static const String routeName = '/binduser';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
